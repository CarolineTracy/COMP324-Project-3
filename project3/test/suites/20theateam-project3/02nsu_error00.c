/*!tests!
{
   "input": ["1"],
   "exception": "NSU_error"
}
*/

void main() {
    int x = 0; // Low

    int secret;
    fscanf(stdin_hi, "%d", &secret);

    if (secret == 1) {
        x = 5; 
    }
}